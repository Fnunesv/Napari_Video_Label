Screenshots referenced by the tutorial pages in `../*.md` go here.

Naming convention: `<page-number>-<slug>.png`, matching the page that
references it (e.g. `01-merge.png` for `docs/01-merge.md`). Each tutorial
page has an HTML comment marking exactly what to capture for its image(s).
